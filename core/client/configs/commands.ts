import { blackout } from "../cmd";
import { car } from "../cmd";
import { coords } from "../cmd";
import { respawn } from "../cmd";
import { revive } from "../cmd";
import { setHp } from "../cmd";
import { setTime } from "../cmd";
import { weapons } from "../cmd";

export const commands = {
  blackout,
  car,
  coords,
  respawn,
  revive,
  setHp,
  setTime,
  weapons,
};
